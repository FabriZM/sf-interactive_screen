Drop your Premiere Pro screenshot here as premiere.png and it will load automatically.

Drop the contact photo for the call screen here as contacto.jpg (or .png) and
the actor's phone will use it — unless a photo has been picked from that
phone's own library, which wins.
